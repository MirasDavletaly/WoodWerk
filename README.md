# WOODWERK — сайт производителя мебели из массива дерева

Статический сайт по образцу структуры **lathome.ru**, но со своим брендом, палитрой
и наполнением: производство мебели и интерьерных решений из массива.

Никаких сборщиков и зависимостей — чистые HTML, CSS и JavaScript.
Чтобы посмотреть, просто откройте `index.html` в браузере.

---

## Структура

```
woodwerk сайт/
├── index.html          Главная: герой, коллекции, категории, о компании,
│                       этапы, галерея проектов, породы, отзывы, FAQ, заявка
├── catalog.html        Каталог с фильтрами по категории и породе + сортировка
├── about.html          О компании и производство (#production)
├── delivery.html       Оплата, доставка, монтаж, гарантия (#warranty), уход
├── partnership.html    Сотрудничество: дизайнерам, архитекторам, салонам
├── contacts.html       Контакты, реквизиты, карта
├── privacy.html        Политика конфиденциальности, оферта (#terms), реквизиты (#requisites)
├── sitemap.html        Карта сайта для посетителей
├── robots.txt
├── sitemap.xml
├── _headers            Заголовки безопасности: Netlify / Cloudflare Pages / Vercel
├── .htaccess           То же для Apache (внутри — блок для nginx)
├── .nojekyll           Чтобы GitHub Pages не прятал файлы, начинающиеся с _ и .
├── .well-known/
│   └── security.txt    Контакт для сообщений об уязвимостях (RFC 9116)
└── assets/
    ├── css/style.css   Вся вёрстка: дизайн-система, компоненты, адаптив
    ├── js/main.js      Меню, фильтры, слайдер, лайтбокс, формы, анимации
    └── img/
        ├── logo.svg
        ├── wood/       Текстуры пород (сгенерированные SVG)
        └── scenes/     Иллюстрации интерьеров (SVG)
```

## Что уже работает

| Блок | Поведение |
|---|---|
| Шапка | Прилипает при прокрутке, мегаменю каталога, отдельное мобильное меню |
| Герой | Полноэкранный, счётчики анимируются при появлении |
| Коллекции / категории | Ссылки ведут в каталог с предустановленным фильтром (`catalog.html?cat=kitchen`) |
| Каталог | Фильтр по категории и породе, сортировка по цене и названию, счётчик и пустое состояние |
| Галерея | Лайтбокс со стрелками, клавишами ← → и Esc |
| Отзывы | Горизонтальный слайдер с кнопками |
| FAQ | Аккордеон |
| Формы | Маска телефона `+7 (___) ___-__-__`, валидация, экран «Заявка отправлена» |
| Прочее | Анимации появления при скролле, кнопка «наверх», модальное окно заявки |

## Что нужно заменить перед публикацией

1. **Контакты.** Телефон уже настоящий — `+7 (747) 902-44-01`, ссылки ведут на
   `tel:+77479024401`. А вот почта `info@woodwerk.ru`, адрес
   `Москва, ул. Складочная, 1, стр. 18` и реквизиты ООО «ВУДВЕРК» — по-прежнему заглушки.
   Ищите их в `index.html` (шапка, блок заявки, подвал), `contacts.html`, `privacy.html`.

   > `+7 747` — казахстанский код, а в текстах сайта указана Москва. Если компания
   > в Казахстане, поправьте заодно адрес, часовой пояс в графике работы и реквизиты.
2. **Ссылки на соцсети.** В шапке и подвале стоят `href="#"`.
3. **Изображения.** Все SVG в `assets/img/` — сгенерированные текстуры и векторные
   сцены-заглушки. Замените их фотографиями реальных работ (те же имена файлов —
   и ничего править в разметке не придётся; для фото используйте `.webp` или `.jpg`
   и поменяйте расширения в `src`).
4. **Отправка форм.** Сейчас `assets/js/main.js` имитирует отправку. Найдите комментарий
   `// Здесь подключается реальная отправка` и подставьте свой запрос — данные уже
   собраны и очищены в переменной `payload`:

   ```js
   fetch('/api/lead', {
     method: 'POST',
     credentials: 'same-origin',
     headers: { 'Content-Type': 'application/json', 'X-CSRF-Token': csrfToken },
     body: JSON.stringify(payload)
   })
   ```

   Клиентские проверки — только удобство для человека. Всё, что важно, дублируйте
   на сервере: см. раздел «Безопасность» ниже.
5. **Карта на `contacts.html`.** Виджет Яндекс.Карт центрирован по приблизительным
   координатам — вставьте свой код встраивания с точной точкой.
6. **Домен в `robots.txt` и `sitemap.xml`** (`https://woodwerk.ru`).

## Настройка внешнего вида

Вся палитра и типографика — в переменных в начале `assets/css/style.css`:

```css
:root {
  --accent: #a9743f;     /* фирменный «карамельный» акцент */
  --ink: #1a1512;        /* основной тёмный */
  --bg: #faf7f2;         /* тёплый фон */
  --dark: #171310;       /* тёмные секции и подвал */
  --container: 1320px;   /* ширина контента */
}
```

Изменение `--accent` перекрашивает кнопки, надзаголовки, иконки и акценты по всему сайту.

## Товары в каталоге

Карточки товаров лежат прямо в `catalog.html`. Чтобы товар участвовал в фильтрах
и сортировке, у `<article class="product">` должны быть атрибуты:

```html
<article class="product" data-cat="kitchen" data-wood="oak"
         data-price="289000" data-name="Кухня «Dubrava Line»">
```

Значения `data-cat` и `data-wood` должны совпадать со значениями `value` у чекбоксов
в блоке фильтров слева.

## Поддержка браузеров

Chrome, Edge, Firefox, Safari — актуальные версии. Используются CSS Grid,
`backdrop-filter`, `IntersectionObserver` и `clamp()`. Учтён
`prefers-reduced-motion` — при системной настройке анимации отключаются.

---

## Безопасность

Сайт статический, поэтому основные риски — это внедрение чужого кода на странице,
спам через формы и утечка данных при переходах наружу. Что сделано:

**Content Security Policy.** В `<head>` каждой страницы прописан строгий CSP:
скрипты — только свои (`script-src 'self'`), стили — свои плюс Google Fonts,
картинки — свои и `data:`, фреймы — только Яндекс.Карты, отправка форм — только
на свой домен. `'unsafe-inline'` не используется: ради этого все inline-стили
вынесены в классы-утилиты (раздел 22 в `style.css`). Любой посторонний скрипт,
попавший в разметку, просто не выполнится.

**Заголовки сервера.** `_headers` (Netlify, Cloudflare Pages, Vercel) и `.htaccess`
(Apache; внутри — готовый блок для nginx) добавляют то, чего нельзя выставить
метатегом: HSTS, `X-Frame-Options: DENY`, `frame-ancestors 'none'` от кликджекинга,
`Permissions-Policy` (камера, микрофон, геолокация выключены), `nosniff`,
`Cross-Origin-Opener-Policy`, скрытие версии сервера и запрет листинга каталогов.

> На GitHub Pages свои заголовки задать нельзя — там работает только CSP из метатега.
> Для полного набора нужен Netlify, Cloudflare Pages или собственный сервер.

**Формы.** Honeypot-поле `company`, скрытое от людей; отсечка отправок быстрее
1,5 секунды после загрузки; не больше 5 отправок за 10 минут с вкладки;
ограничения `maxlength` на всех полях; вырезание управляющих символов;
защита от загрязнения прототипа (`__proto__`, `constructor`, `prototype`
в payload не попадают).

**JavaScript.** Нигде не используется `innerHTML` — только `textContent`, поэтому
XSS через данные невозможен. Параметры из адресной строки (`?cat=`, `?wood=`)
не подставляются в CSS-селектор, а сравниваются со списком существующих
фильтров — иначе это была бы инъекция в `querySelector`.

**Ссылки и фреймы.** Всем внешним ссылкам проставляется `rel="noopener noreferrer"`
(в разметке и дополнительно из JS), у iframe Яндекс.Карт стоит `sandbox`
и `referrerpolicy`. Метатег `referrer` ограничивает передачу адреса
до `strict-origin-when-cross-origin`.

**Куда сообщать об уязвимостях.** `.well-known/security.txt` по RFC 9116.

### Чего клиентский код не может — сделайте на сервере

Всё перечисленное выше обходится через консоль браузера. Когда подключите
реальный приём заявок, обязательно продублируйте на бэкенде:

- валидацию имени, телефона и длины комментария;
- CSRF-токен (в `main.js` уже показано, куда его класть) и `SameSite=Strict` для cookie;
- ограничение частоты запросов по IP;
- защиту от спама вроде reCAPTCHA или Cloudflare Turnstile;
- параметризованные запросы к БД и экранирование при выводе заявок в админку;
- HTTPS с актуальным сертификатом.
