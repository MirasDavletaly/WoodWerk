/* Конфигурация фронтенда.
 *
 * leadEndpoint — адрес, куда уходят заявки с форм.
 * Пустая строка = демо-режим: форма проверяется, но никуда не отправляется.
 *
 * apiBase — корень API каталога и админ-панели. Каталог, страница изделия
 * и панель управления берут данные из базы через этот адрес.
 *
 * Go-сервер из папки server/ подменяет этот файл на лету и подставляет
 * рабочие значения, поэтому при запуске через него править ничего не нужно.
 * Для другого бэкенда впишите свои адреса руками.
 */
window.WOODWERK = {
  leadEndpoint: "",
  apiBase: "/api"
};
